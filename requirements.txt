# --- Web framework ---
Flask==3.1.0
python-dotenv==1.0.1
Flask-Cors==5.0.0
Flask-Limiter==3.8.0

# --- Database ---
# No Alembic: the models are the single source of truth and `flask init-db`
# creates the schema from them.
SQLAlchemy==2.0.36
Flask-SQLAlchemy==3.1.1
psycopg[binary]==3.2.3

# --- Validation ---
pydantic==2.10.3
email-validator==2.2.0

# --- Auth ---
Flask-JWT-Extended==4.7.1

# --- Images / Cloudinary ---
cloudinary==1.41.0
Pillow==11.0.0
requests==2.32.3
opencv-python-headless==4.13.0.90
numpy==2.5.2

# --- Model (YOLO11m, weights/best.pt) ---
# Must be >=8.3 - the weights are YOLO11, which earlier Ultralytics cannot load,
# despite DOCUMENTATION.md describing the model as YOLOv8.
ultralytics==8.4.121
# Install CPU-only torch first to avoid pulling ~2.5GB of CUDA wheels:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
torchvision==0.28.0

# --- Tests ---
pytest==8.3.4
pytest-cov==6.0.0

# --- Production server (Windows-friendly) ---
waitress==3.0.2
